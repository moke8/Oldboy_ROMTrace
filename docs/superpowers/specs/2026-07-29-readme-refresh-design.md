# README 更新设计

## 目标

根据 README 上次修改后的 Git 提交和当前已实现的 PS1 字典功能，补充用户可见能力与项目结构说明，不重排 README 现有章节。

## 更新范围

- 扩充“完整刮削日志”，说明翻译失败会输出脱敏后的中文原因，包括模型不可用、权限不足、网络、超时、配置和响应异常。
- 扩充“多语言支持”，说明翻译可选择关闭、Google 翻译或 OpenAI 兼容 AI 翻译；AI 可配置中转站、模型和 Key，失败返回原文且不中断批量任务。
- 新增简短“翻译配置”小节，说明三种选项、AI 配置字段、地址兼容形式和安全行为。
- 更新 PS1 平台说明，明确从 `SYSTEM.CNF` 提取序列号后通过内置字典解析标准英文名，未知序列号保留原始标题。
- 在项目结构中补充 `ps1_game_db.py` 与 `build_ps1_db.py`，说明字典数据和生成脚本职责。

## 边界

不添加逐提交更新日志，不描述尚未实现的功能，不修改安装、构建、License 或作者信息。README 不展示真实中转站、模型或 Key。

## 验证

检查 README 描述与当前代码一致，运行 Markdown 差异检查，并确认没有敏感配置进入提交。
